rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para a operação corporativa compartilhada
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
